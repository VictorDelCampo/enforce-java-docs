min_version: 1.1.4

pre-commit:
  parallel: true
  commands:
    check-javadocs:
      tags: check javadocs
      root: backend/
      run: ./gradlew checkstyleMain -include=$(echo {staged_files} | sed 's/ /,/g') && git add {staged_files}
